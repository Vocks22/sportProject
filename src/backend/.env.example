# Exemple de configuration pour développement local avec base Render

# Option 1: Développement avec base Render (RECOMMANDÉ)
# Obtenez l'URL depuis: Render Dashboard → diettracker-db → Connect → External Connection
DATABASE_URL=postgresql://diettracker:PASSWORD@HOST.oregon-postgres.render.com/diettracker

# Option 2: Développement avec PostgreSQL local
# DATABASE_URL=postgresql://diettracker:dev123@localhost/diettracker_dev

# Configuration Flask
FLASK_ENV=development
FLASK_DEBUG=1
FLASK_APP=main.py

# Optionnel
SECRET_KEY=dev-secret-key-change-in-production
JWT_SECRET_KEY=dev-jwt-secret-change-in-production